language: php
php:
  - '7.0'
env:
  - DB=mysql
  - DB=pgsql
matrix:
  exclude:
    - php: hhvm
      env: DB=pgsql  # PDO driver for pgsql is unsupported by HHVM (3rd party install for support)
  allow_failures:
    - php: 7.0